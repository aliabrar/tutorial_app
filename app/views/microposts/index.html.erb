<h1>
  <%= gravatar_for @user %>
  <%= link_to @user.name, user_path(@user) %>'s Microposts
</h1>
        <table class="microposts" summary="User microposts">
          <%= render @microposts %>
        </table>
        <%= will_paginate @microposts %>

